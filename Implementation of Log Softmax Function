import numpy as np

def log_softmax(scores: list) -> np.ndarray:
	max_score = max(scores)
	x = np.array(scores)
	exponents = np.exp(x - max_score)
	sum_exp = sum(exponents)
	log_val = np.log(sum_exp)
	return x - max_score - log_val
