import math

def softmax(scores: list[float]) -> list[float]:
    max_score = max(scores)
    exponents = [math.exp(score - max_score) for score in scores]
    sum_exponents = sum(exponents)
    return [exp / sum_exponents for exp in exponents]
