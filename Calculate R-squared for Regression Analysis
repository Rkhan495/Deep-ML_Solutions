import numpy as np

def r_squared(y_true, y_pred):
	if len(y_true) != len(y_pred):
		return 

	SSR = 0.0
	SST = 0.0

	mean_true = sum(y_true) / len(y_true)

	for i in range(len(y_true)):
		SSR += ((y_true[i] - y_pred[i]) ** 2)
		SST += ((y_true[i] - mean_true) ** 2)

	return 1 - (SSR / SST)
