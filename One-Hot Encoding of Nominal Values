import numpy as np

def to_categorical(x, n_col=None):
	if n_col == None: n_col = len(set(x))
	result = np.zeros((len(x), n_col))

	for i in range(len(x)):
		for j in range(n_col):
			result[i][x[i]] = 1

	return result
